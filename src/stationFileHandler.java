public class stationFileHandler {
    IRNstationManager manager;

     /**
     * handles all things station-
     * file writing
     * file reading
     */
    public stationFileHandler(IRNstationManager manager) {
        this.manager = manager;
    }
}
